FROM node:18-alpine
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci --only=production

# Copy app source
COPY . .

# Cloud Run injects PORT environment variable
ENV PORT=8080
EXPOSE 8080

CMD ["node", "server.js"]
