# ============================================================
# BlueBikes MLOps Project - Environment Configuration
# ============================================================
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in all the values below
# 3. NEVER commit the .env file to Git!
# ============================================================

# ------------------------------------------------------------
# NOAA Weather API (Required for data collection)
# ------------------------------------------------------------
# Get your free API key from: https://www.ncdc.noaa.gov/cdo-web/token
NOAA_API_KEY=your_noaa_api_key_here

# ------------------------------------------------------------
# Discord Notifications (Optional but recommended)
# ------------------------------------------------------------
# Create a webhook in your Discord server:
# Server Settings -> Integrations -> Webhooks -> New Webhook
# Leave empty to disable Discord notifications
DISCORD_WEBHOOK_URL=

# ------------------------------------------------------------
# GitHub Integration (Required for model releases)
# ------------------------------------------------------------
# Format: username/repository-name
GITHUB_REPO=your-username/your-repo-name

# Create a Personal Access Token with 'repo' scope:
# GitHub -> Settings -> Developer settings -> Personal access tokens
GITHUB_TOKEN=your_github_token_here

# ------------------------------------------------------------
# Google Cloud Platform (Required for cloud features)
# ------------------------------------------------------------
# Create a GCS bucket for storing models and data
# Go to: https://console.cloud.google.com/storage/browser
GCS_MODEL_BUCKET=your-gcs-bucket-name

# Prefix paths within the bucket (usually don't need to change)
GCS_MODEL_PREFIX=models/production
GCS_DATA_PREFIX=data

# ------------------------------------------------------------
# Airflow Configuration (Optional - defaults work fine)
# ------------------------------------------------------------
# User ID for Airflow (run: id -u to get your user ID)
AIRFLOW_UID=50000

# Airflow web interface credentials
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow