version: '3.9'

services:
  pipeline:
    build:
      context: .
      dockerfile: Dockerfile.pipeline
    container_name: ml_pipeline_standalone
    volumes:
      - ./data_pipeline/data:/app/data
      - ./data_pipeline/logs:/app/logs
    command: python data_pipeline/scripts/datapipeline.py